import 'package:flutter/material.dart';

class InteractionCheckerScreen extends StatelessWidget {
  const InteractionCheckerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Interaction Checker')),
      body: const Center(child: Text('Drug Interaction Checker - Coming Soon')),
    );
  }
}
